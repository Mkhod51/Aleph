/**
 * The single source of truth for the product name (doc 00 decision log; doc 10 §4).
 * Nothing else in the codebase should hard-code the product name — a future rename
 * is a one-line change here.
 */
export const APP_NAME = 'Aleph';

/** Wordmark glyph placeholder (doc 07 §4 uses ◆; doc 10 §4 suggests א / ℵ₀ later). */
export const APP_MARK = '◆';
